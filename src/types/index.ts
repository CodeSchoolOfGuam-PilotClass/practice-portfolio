export interface SiteMeta {
  title: string;
  description: string;
}

export interface Project {
  title: string;
  description: string;
  techStack: string[];
  image: string;
  imageAlt: string;
  liveUrl: string;
  repoUrl: string;
}